#include "main.h"
/**
 * print _alphabet -  A function that prints the alphabet
 * written by TJ
 * Return: 0 (SUCCESS)
 */
void  print_alphabet(void)

{
char alphabet;

for (alphabet = 'a'; alphabet <= 'z'; alphabet++)
_putchar(alphabet);

_putchar('\n')
return (0);
}
